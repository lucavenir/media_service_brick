import 'package:mason/mason.dart';

void run(HookContext context) {
  // TODO: add pre-generation logic.
  // add pre-gen logic to determine wheter or not to add hooks related dependencies
  // add pre-gen logic to determine which media dependencies to add
}
